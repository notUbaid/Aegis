{
  "name": "aegis",
  "version": "0.1.0",
  "private": true,
  "description": "Aegis — Agentic safety platform for mass gatherings.",
  "scripts": {
    "dev:staff": "npm --workspace apps/staff run dev",
    "dev:dashboard": "npm --workspace apps/dashboard run dev",
    "build:staff": "npm --workspace apps/staff run build",
    "build:dashboard": "npm --workspace apps/dashboard run build",
    "lint": "npm --workspace apps/staff run lint && npm --workspace apps/dashboard run lint"
  }
}
